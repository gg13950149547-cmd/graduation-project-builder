interface:
  display_name: "毕业设计总包"
  short_description: "Cross-stack autopilot for build, polish, and verification."
  default_prompt: "Use $graduation-project-builder to summarize requirements, build missing parts, polish the project, and verify it end to end across the current stack."
policy:
  allow_implicit_invocation: true
